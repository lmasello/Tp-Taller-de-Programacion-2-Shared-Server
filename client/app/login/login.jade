header
    img(src='public/assets/images/logo.png')

section
    form(ng-click="$ctrl.facebookLogin()")
        a.btn-facebook LOG IN WITH FACEBOOK

p Or..

section
    form(data-ng-submit="$ctrl.submitLogin()")
        input(type='email',
        name='email'
        placeholder="test@music-io.com",
        data-ng-model="$ctrl.data.email",
        )
        input(type='password',
        name='password',
        placeholder="Password",
        data-ng-model="$ctrl.data.password",
        required)
        input(type='submit', value="Login")